<p><%= work.title %></p>
          <div class="w3-light-grey w3-round-xlarge w3-small">
            <div class="w3-container w3-center w3-round-xlarge w3-teal" style="width:<%= work.position %>"><%= work.position %></div>
          </div>

<p>
  
  <%= link_to 'Destroy Skill', [work.resume, work],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</p>